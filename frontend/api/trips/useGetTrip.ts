/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../client";
import type { RequestConfig, ResponseErrorConfig } from "../client";
import type {
  GetTripQueryResponse,
  GetTripPathParams,
  GetTrip400,
  GetTrip404,
  GetTrip500,
} from "../../types/types.gen.ts";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getTripQueryKey = (tripID: GetTripPathParams["tripID"]) =>
  [{ url: "/api/v1/trips/:tripID", params: { tripID: tripID } }] as const;

export type GetTripQueryKey = ReturnType<typeof getTripQueryKey>;

/**
 * @description Retrieves a trip by ID
 * @summary Get a trip
 * {@link /api/v1/trips/:tripID}
 */
export async function getTrip(
  tripID: GetTripPathParams["tripID"],
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetTripQueryResponse,
    ResponseErrorConfig<GetTrip400 | GetTrip404 | GetTrip500>,
    unknown
  >({ method: "GET", url: `/api/v1/trips/${tripID}`, ...requestConfig });
  return res.data;
}

export function getTripQueryOptions(
  tripID: GetTripPathParams["tripID"],
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const queryKey = getTripQueryKey(tripID);
  return queryOptions<
    GetTripQueryResponse,
    ResponseErrorConfig<GetTrip400 | GetTrip404 | GetTrip500>,
    GetTripQueryResponse,
    typeof queryKey
  >({
    enabled: !!tripID,
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return getTrip(tripID, config);
    },
  });
}

/**
 * @description Retrieves a trip by ID
 * @summary Get a trip
 * {@link /api/v1/trips/:tripID}
 */
export function useGetTrip<
  TData = GetTripQueryResponse,
  TQueryData = GetTripQueryResponse,
  TQueryKey extends QueryKey = GetTripQueryKey,
>(
  tripID: GetTripPathParams["tripID"],
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetTripQueryResponse,
        ResponseErrorConfig<GetTrip400 | GetTrip404 | GetTrip500>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: typeof fetch };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...queryOptions } = queryConfig;
  const queryKey = queryOptions?.queryKey ?? getTripQueryKey(tripID);

  const query = useQuery(
    {
      ...getTripQueryOptions(tripID, config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<GetTrip400 | GetTrip404 | GetTrip500>
  > & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
