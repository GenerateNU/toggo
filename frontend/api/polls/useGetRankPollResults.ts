/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../client";
import type { Client, RequestConfig, ResponseErrorConfig } from "../client";
import type {
  GetRankPollResultsQueryResponse,
  GetRankPollResultsPathParams,
  GetRankPollResults400,
  GetRankPollResults401,
  GetRankPollResults403,
  GetRankPollResults404,
  GetRankPollResults500,
} from "../../types/types.gen.ts";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getRankPollResultsQueryKey = (
  tripID: GetRankPollResultsPathParams["tripID"],
  pollId: GetRankPollResultsPathParams["pollId"],
) =>
  [
    {
      url: "/api/v1/trips/:tripID/rank-polls/:pollId",
      params: { tripID: tripID, pollId: pollId },
    },
  ] as const;

export type GetRankPollResultsQueryKey = ReturnType<
  typeof getRankPollResultsQueryKey
>;

/**
 * @description Retrieves a rank poll with Borda count scores and user's personal ranking
 * @summary Get rank poll results
 * {@link /api/v1/trips/:tripID/rank-polls/:pollId}
 */
export async function getRankPollResults(
  tripID: GetRankPollResultsPathParams["tripID"],
  pollId: GetRankPollResultsPathParams["pollId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetRankPollResultsQueryResponse,
    ResponseErrorConfig<
      | GetRankPollResults400
      | GetRankPollResults401
      | GetRankPollResults403
      | GetRankPollResults404
      | GetRankPollResults500
    >,
    unknown
  >({
    method: "GET",
    url: `/api/v1/trips/${tripID}/rank-polls/${pollId}`,
    ...requestConfig,
  });
  return res.data;
}

export function getRankPollResultsQueryOptions(
  tripID: GetRankPollResultsPathParams["tripID"],
  pollId: GetRankPollResultsPathParams["pollId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getRankPollResultsQueryKey(tripID, pollId);
  return queryOptions<
    GetRankPollResultsQueryResponse,
    ResponseErrorConfig<
      | GetRankPollResults400
      | GetRankPollResults401
      | GetRankPollResults403
      | GetRankPollResults404
      | GetRankPollResults500
    >,
    GetRankPollResultsQueryResponse,
    typeof queryKey
  >({
    enabled: !!(tripID && pollId),
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return getRankPollResults(tripID, pollId, config);
    },
  });
}

/**
 * @description Retrieves a rank poll with Borda count scores and user's personal ranking
 * @summary Get rank poll results
 * {@link /api/v1/trips/:tripID/rank-polls/:pollId}
 */
export function useGetRankPollResults<
  TData = GetRankPollResultsQueryResponse,
  TQueryData = GetRankPollResultsQueryResponse,
  TQueryKey extends QueryKey = GetRankPollResultsQueryKey,
>(
  tripID: GetRankPollResultsPathParams["tripID"],
  pollId: GetRankPollResultsPathParams["pollId"],
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetRankPollResultsQueryResponse,
        ResponseErrorConfig<
          | GetRankPollResults400
          | GetRankPollResults401
          | GetRankPollResults403
          | GetRankPollResults404
          | GetRankPollResults500
        >,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...queryOptions } = queryConfig;
  const queryKey =
    queryOptions?.queryKey ?? getRankPollResultsQueryKey(tripID, pollId);

  const query = useQuery(
    {
      ...getRankPollResultsQueryOptions(tripID, pollId, config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<
      | GetRankPollResults400
      | GetRankPollResults401
      | GetRankPollResults403
      | GetRankPollResults404
      | GetRankPollResults500
    >
  > & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
