/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../client";
import type { Client, RequestConfig, ResponseErrorConfig } from "../client";
import type {
  SearchTripsQueryResponse,
  SearchTripsQueryParams,
  SearchTrips400,
  SearchTrips401,
  SearchTrips422,
  SearchTrips500,
} from "../../types/types.gen.ts";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const searchTripsQueryKey = (params: SearchTripsQueryParams) =>
  [{ url: "/api/v1/search/trips" }, ...(params ? [params] : [])] as const;

export type SearchTripsQueryKey = ReturnType<typeof searchTripsQueryKey>;

/**
 * @description Full-text search over trips the authenticated user is a member of. Ordered by relevance then recency.
 * @summary Search trips
 * {@link /api/v1/search/trips}
 */
export async function searchTrips(
  params: SearchTripsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    SearchTripsQueryResponse,
    ResponseErrorConfig<
      SearchTrips400 | SearchTrips401 | SearchTrips422 | SearchTrips500
    >,
    unknown
  >({ method: "GET", url: `/api/v1/search/trips`, params, ...requestConfig });
  return res.data;
}

export function searchTripsQueryOptions(
  params: SearchTripsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = searchTripsQueryKey(params);
  return queryOptions<
    SearchTripsQueryResponse,
    ResponseErrorConfig<
      SearchTrips400 | SearchTrips401 | SearchTrips422 | SearchTrips500
    >,
    SearchTripsQueryResponse,
    typeof queryKey
  >({
    enabled: !!params,
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return searchTrips(params, config);
    },
  });
}

/**
 * @description Full-text search over trips the authenticated user is a member of. Ordered by relevance then recency.
 * @summary Search trips
 * {@link /api/v1/search/trips}
 */
export function useSearchTrips<
  TData = SearchTripsQueryResponse,
  TQueryData = SearchTripsQueryResponse,
  TQueryKey extends QueryKey = SearchTripsQueryKey,
>(
  params: SearchTripsQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        SearchTripsQueryResponse,
        ResponseErrorConfig<
          SearchTrips400 | SearchTrips401 | SearchTrips422 | SearchTrips500
        >,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...queryOptions } = queryConfig;
  const queryKey = queryOptions?.queryKey ?? searchTripsQueryKey(params);

  const query = useQuery(
    {
      ...searchTripsQueryOptions(params, config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<
      SearchTrips400 | SearchTrips401 | SearchTrips422 | SearchTrips500
    >
  > & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
