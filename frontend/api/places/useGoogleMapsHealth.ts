/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../client";
import type { Client, RequestConfig, ResponseErrorConfig } from "../client";
import type {
  GoogleMapsHealthQueryResponse,
  GoogleMapsHealth500,
} from "../../types/types.gen.ts";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const googleMapsHealthQueryKey = () =>
  [{ url: "/api/v1/search/places/health" }] as const;

export type GoogleMapsHealthQueryKey = ReturnType<
  typeof googleMapsHealthQueryKey
>;

/**
 * @description Checks if Google Maps API is connected and accessible
 * @summary Google Maps health check
 * {@link /api/v1/search/places/health}
 */
export async function googleMapsHealth(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GoogleMapsHealthQueryResponse,
    ResponseErrorConfig<GoogleMapsHealth500>,
    unknown
  >({ method: "GET", url: `/api/v1/search/places/health`, ...requestConfig });
  return res.data;
}

export function googleMapsHealthQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = googleMapsHealthQueryKey();
  return queryOptions<
    GoogleMapsHealthQueryResponse,
    ResponseErrorConfig<GoogleMapsHealth500>,
    GoogleMapsHealthQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return googleMapsHealth(config);
    },
  });
}

/**
 * @description Checks if Google Maps API is connected and accessible
 * @summary Google Maps health check
 * {@link /api/v1/search/places/health}
 */
export function useGoogleMapsHealth<
  TData = GoogleMapsHealthQueryResponse,
  TQueryData = GoogleMapsHealthQueryResponse,
  TQueryKey extends QueryKey = GoogleMapsHealthQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        GoogleMapsHealthQueryResponse,
        ResponseErrorConfig<GoogleMapsHealth500>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...queryOptions } = queryConfig;
  const queryKey = queryOptions?.queryKey ?? googleMapsHealthQueryKey();

  const query = useQuery(
    {
      ...googleMapsHealthQueryOptions(config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<GoogleMapsHealth500>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
