import { Text } from "@/design-system";

// should include the banner image and the map icon in the top right corner (should have a 'header' box)
export default function TripDetailBanner() {
    return (
        <Text>
            Some banner image and stuff
        </Text>
    );
};
