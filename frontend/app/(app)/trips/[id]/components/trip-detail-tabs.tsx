import { Text } from "@/design-system";

// should take in tabs as props and render them in a horizontal scroll view
// expecting the fetch to happen from the screen
export default function TripDetailTabs() {
    return (
        <Text>
            some tabs
        </Text>
    );
};
