import { Text } from "@/design-system";

// This will eventually replace the index.tsx page. I'm just putting it in this directory for now


// Note - do not remove - the 'itinerary' tab selection defaults to this page (trips index root)

export default function TripDetailPage() {

    // TODO: fetching logic for the trip details and pass it to props. the itinerary content will be fetched
    // separately.
    return (
        <Text>
            Some banner image and stuff
        </Text>
    );
};
