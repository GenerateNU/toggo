{
  "type": "object",
  "required": ["content", "entity_id", "entity_type", "trip_id"],
  "properties": {
    "content": { "type": "string", "minLength": 1 },
    "entity_id": { "type": "string" },
    "entity_type": {
      "enum": ["activity", "pitch"],
      "allOf": [
        {
          "type": "string",
          "enum": ["activity", "pitch"],
          "x-enum-varnames": ["ActivityEntity", "PitchEntity"],
          "x-readme-ref-name": "models.EntityType"
        }
      ]
    },
    "trip_id": { "type": "string" }
  },
  "x-readme-ref-name": "models.CreateCommentRequest"
}
